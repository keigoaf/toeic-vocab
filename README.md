# TOEIC 単語トレーナー (PWA)

TOEICの単語・熟語を **間隔反復（Leitnerボックス方式）** で暗記するためのアプリです。
スマホのホーム画面に置いて、ネイティブアプリのように使えます（オフライン対応・端末内に進捗を永続保存）。

最終更新: 2026-07-29 14:20 (JST) / バージョン **3.8.0**

---

## 主な機能

- **間隔反復**: 正解した語は上のボックスへ進み、次の出題までの間隔が延びる。不正解ならBox1へ戻る。
- **出題順（v2.2.0）**: 1日の境目は**朝6時**。同じ日なら何時に開いても同じ顔ぶれが出ます。順番は次の通り:
  1. **復習分**（Box2以上・前日までに正解した語）— ボックスの大きい順
  2. **再挑戦分**（前回不正解だった語）
  3. **新規**（まだ解いていない語）— 登録順
  不正解の語は当日は繰り返さず、翌日の「再挑戦分」として出ます。
- **1日の新規語数**: 設定画面で上限を指定できます（既定20語）。復習分と再挑戦分は上限の対象外で必ず出題されるため、未消化の語が積み残ることはありません。
- **出題周期の設定**: 各ボックスの「次回出題まで何日後か」を設定画面から自由に変更可能（初期値: 当日/1/3/7/14/30/60日）。
- **単語管理**: 追加・編集・削除・検索。単語には **品詞**（名詞・動詞など）を付けられ、めくった裏面に表示。
- **一括インポート / エクスポート**: テキストの貼り付けでまとめて登録・差し替え・バックアップ。
- **JSON一括取り込み**（v1.3.0）: 「単語」→「一括インポート」から、同梱の **TOEIC頻出500問**（`toeic-500.json`）をワンタップ追加。自作の JSON ファイル読み込みにも対応。重複（同じ単語）は自動でスキップ。
- **発音（v1.6.0）**: カードをめくった裏面のスピーカーボタン（🔊）で、単語と例文を読み上げます。ブラウザ標準の音声合成を使うため追加ファイル・通信は不要で、オフラインでも動作します。
- **例文の和訳（v1.7.0）**: 裏面の例文の下に日本語訳を表示。同梱1500問すべてに訳が付いています。
- **バックアップ / 復元（v1.7.0）**: 設定画面から、学習の進み具合（ボックス・出題日・正誤回数）ごとJSONに保存でき、復元もできます。端末変更やデータ消失に備えられます。
- **単語Tips（v3.7.0）**: カードの裏（および単語一覧の詳細）に、タップで開く解説ボタンを表示します。2種類あります。
  - **🔍 使い分け**: 日本語では同じ訳になるのに使い分けが要る語の比較（例: `congestion` と `crowd` / `fee` と `fare` と `charge` / `assure` と `ensure` と `insure`）。
  - **🔗 意味のつながり**: 同じ語なのに意味が離れていて疑問が出る語の解説（例: `commence`「開始する」なのに `commencement` が「卒業式」なのはなぜか / `board` が「板・役員会・搭乗」になる理由 / `outstanding` が「傑出した」と「未払いの」を兼ねる理由）。
  1件のTipsを複数の見出し語に紐づけているため、比較する語のどれから開いても同じ解説が読めます。Tipsのある語には単語一覧に `Tips` バッジが付きます。データは `word-relations.json` の `tips`。
- **シャドーイング（v3.8.0）**: 左上のモードメニューから「シャドーイング」を選ぶと、専用ページ `shadowing.html` が開きます。
  - TOEIC Part 3/4 を意識した **30秒〜2分** の英文（初版5本：空港アナウンス / 留守番電話 / 社内説明 / ラジオ広告 / 工場見学）。
  - **再生速度** 0.5〜1.3倍。**英文・和訳の表示を個別にON/OFF**。表示ONなら、読み上げに合わせて**語ごとに色が動きます**（カラオケ式）。
  - **練習ステップ**をワンタップ切替：① 聞く（英文を伏せ和訳で内容把握）→ ② 重ねる（オーバーラッピング・0.8倍）→ ③ 追う（英文を伏せてシャドーイング）→ ④ 再現（1文ずつ間を置いてリピーティング）。
  - 文をタップするとその文から再生。文ごとのくり返し（1〜3回）、文間の無音（0〜3秒）、全体ループ、教材ごとの練習回数記録。
  - 音声は端末内蔵の音声合成なので、音声ファイルの通信はなくオフラインでも動きます。教材データは `shadowing.json`。
- **進捗の永続保存**: ブラウザの `localStorage` に保存。アプリを閉じても残る（同一URL内で保持）。

---

## ファイル構成

配信するときは、以下を **同じフォルダの直下** に置いてください。

```
toeic-vocab/
├─ index.html                アプリ本体
├─ shadowing.html            シャドーイング練習ページ（v3.8.0）
├─ shadowing.json            シャドーイング教材データ（v3.8.0・初版5本）
├─ manifest.json             PWA設定（アプリ名・アイコン等）
├─ sw.js                     Service Worker（オフライン動作）
├─ toeic-500.json            同梱のTOEIC頻出500問データ
├─ toeic-1000.json           追加のTOEIC頻出1000問データ（500と重複なし）
├─ toeic-extra.json          追加54問データ（上記1500と重複なし）
├─ toeic-extra2.json         法務・契約・製造・品質管理の137問（他と重複なし）
├─ toeic-new1000.json        新規1000問（他すべてと重複なし・全和訳付き）
├─ toeic-new2.json           新規169問 第2弾（既存すべてと重複なし・全和訳付き）
├─ word-relations.json       派生語グループ・語源解説・単語Tipsのデータ
├─ part5.json                TOEIC Part5 問題（330問・解説つき）
├─ part6.json                TOEIC Part6 問題（75文書300問・解説つき）
├─ icon-192.png              アイコン
├─ icon-512.png              アイコン
├─ icon-180.png              iOSホーム画面用アイコン
├─ icon-maskable-512.png     Android maskable用アイコン
└─ README.md                 このファイル
```

---

## スマホで「アプリ」として使う手順

ホーム画面に本物のアプリのように入れるには、**HTTPSで配信**する必要があります（Service Workerの制約）。
一番手軽なのは **GitHub Pages（無料・即HTTPS）** です。

### 1. GitHub Pages に配置する

**Git CLI の場合:**

```bash
cd toeic-vocab
git init
git add .
git commit -m "TOEIC vocab PWA v1.2.0"
git branch -M main
git remote add origin https://github.com/keigoaf/toeic-vocab.git   # ← 自分のリポジトリに
git push -u origin main
```

その後、GitHub のリポジトリで
`Settings → Pages → Build and deployment → Source: Deploy from a branch`
→ Branch を `main` / `/(root)` にして **Save**。

1分ほどで次のURLで公開されます（ユーザー名・リポジトリ名に合わせて読み替え）:

```
https://keigoaf.github.io/toeic-vocab/
```

### 2. ホーム画面に追加する

公開URLをスマホのブラウザで開き、ホーム画面に追加します。

- **iPhone (Safari)**: 共有ボタン → 「ホーム画面に追加」
- **Android (Chrome)**: メニュー(⋮) → 「アプリをインストール」または「ホーム画面に追加」

追加後はアイコンから全画面で起動し、オフラインでも動きます。

### （代替）自分のVPSで配信する場合

`168.144.41.110` 等の自前サーバでも配信できますが、Service Worker を有効にするには
**有効なHTTPS証明書**が必要です（自己署名不可）。Let's Encrypt などで証明書を用意し、
静的ファイルとして上記フォルダを配信してください。手軽さではGitHub Pagesが上です。

---

## 一括インポート / エクスポートのデータ形式

1行に1語。カンマ（または タブ）区切りで、**英語 , 意味 , 品詞/種別 , 例文** の順。
3列目・4列目は省略可能です。

- 3列目に **品詞**（名詞・動詞・形容詞・副詞・前置詞・接続詞・代名詞、または `n.` `v.` `adj.` 等）を書くと単語として登録。
- 3列目に **`熟語` または `idiom`** を書くと熟語として登録（品詞なし）。
- 空欄なら単語（品詞なし）として登録。

例:

```
acquire, 獲得する, 動詞, We plan to acquire a firm.
comply with, 順守する, 熟語
invoice, 請求書, 名詞
tentative, 仮の, adj.
```

エクスポートも同じ形式で出力されるので、そのままインポートに貼り戻せます（往復可能）。

### JSONファイルの形式（v1.3.0）

オブジェクトの配列です。`term` と `meaning` が必須、他は任意。熟語は `"type":"idiom"` を付けます。

```json
[
  { "term": "acquire", "meaning": "獲得する", "type": "word", "pos": "動詞", "example": "They acquired a rival firm." },
  { "term": "comply with", "meaning": "順守する", "type": "idiom", "example": "Please comply with the rules." }
]
```

取り込み方法は3通り（「単語」→「一括インポート」内）:
1. **TOEIC頻出500問を追加** / **追加1000問を追加** / **追加54問を追加** / **法務・製造137問を追加** / **新規1000問を追加** ボタン（同梱データをワンタップ。合計2691問）
2. **JSONを貼り付けて取り込む**（AI出力をそのまま貼り付け。`\`\`\`json` の囲みは自動除去）
3. **JSONファイルを読み込む**（`.json` を選択）

既存と同じ単語は自動でスキップされるので、重ねて取り込んでも増えません。

### AIに問題を作らせるプロンプト

無料のAIに以下を渡すと、そのまま貼り付け取り込みできるJSONが得られます（`【个数】`と`【テーマ】`は書き換え）。出力後、必要なら誤記チェックをしてから貼り付けてください。

```
あなたはTOEIC対策の英単語教材を作る専門家です。
以下の条件でTOEIC頻出の英単語・熟語を【50】個、JSON配列だけで出力してください。

# 出力形式（厳守）
- 出力はJSON配列のみ。前後に説明文やコードフェンス(```)を付けない。
- 各要素は次のキーを持つオブジェクト:
  - "term": 英語の見出し（単語または熟語）
  - "meaning": 日本語の意味（簡潔に。複数の意味は「/」で区切る）
  - "type": "word"（単語）または "idiom"（熟語・句動詞）
  - "pos": 品詞。typeがwordのときのみ。次のいずれか:
           "名詞" "動詞" "形容詞" "副詞" "前置詞" "接続詞" "代名詞"
           typeがidiomのときは "pos" を省略する。
  - "example": その語を使った短い英語例文（10語程度、TOEICらしいビジネス文脈）

# 内容の条件
- TOEIC(600〜800点帯)で頻出の語を優先。
- テーマ: 【ビジネス一般】
- 意味・品詞・例文に誤りがないこと。例文中で必ずtermを使う。
- 同じtermを重複させない。

# 出力例（この形式に厳密に従う）
[
  {"term":"acquire","meaning":"獲得する","type":"word","pos":"動詞","example":"They acquired a rival firm."},
  {"term":"comply with","meaning":"順守する","type":"idiom","example":"Please comply with the rules."}
]
```

---

## バックアップと注意点

- 進捗は `localStorage` に保存されます。これは **配信URL（ドメイン）ごと** に保持されます。
  配信先URLを変えると以前のデータは引き継がれません。
- 大事な単語リストは「単語」→「エクスポート」でテキストを控えておくと安全です。
  復元は「一括インポート」に貼り付けます。
- ブラウザの履歴・サイトデータを消すと `localStorage` も消えます。定期的なエクスポートを推奨。

---

## 開発メモ

- アプリ本体・SW・READMEには先頭に最終更新日時を記載しています。
  起動時にコンソールへ最終更新日時とバージョンを最初に出力し、画面右上にも表示します
  （どのバージョンを動かしているか後から確認できるように）。
- **アプリを更新したとき** は、`sw.js` 内の `CACHE` 名のバージョンも上げてください。
  古いキャッシュが確実に入れ替わります。
- **Tipsを増やすとき**（v3.7.0〜）は `word-relations.json` の `tips` 配列に追記します。
  ```json
  { "id": "一意のID",
    "kind": "nuance",            // nuance=使い分け / origin=意味のつながり
    "title": "見出し（ボタンに出る文）",
    "terms": ["congestion", "congested"],   // 小文字。実在する見出し語のみ
    "body": "本文。\n で改行できます（表示は pre-wrap）。" }
  ```
  `terms` に入れた語すべてのカード裏に同じTipsボタンが出ます。語彙に無い語を書いても
  表示先が無いだけで害はありませんが、実在する語に紐づけてください。
- **シャドーイング教材を増やすとき**（v3.8.0〜）は `shadowing.json` の `passages` に追記します。
  ```json
  { "id": "sh006", "title": "…", "category": "Part 4 / アナウンス",
    "scene": "…", "level": 730,
    "sentences": [ { "en": "英文1文", "ja": "その和訳" } ],
    "vocab": [ { "term": "…", "meaning": "…" } ],
    "tip": "音のポイント" }
  ```
  **1文ずつ区切って持たせること**が重要です。再生・ハイライト・リピートの単位が「文」なので、
  ここで区切っておくと英文と和訳が確実に対応します。目安は 100〜300語（約40秒〜2分）。
  数字は `four five two nine` のように読み方どおりに書くと音声合成が正しく読みます。

---

## アプリの更新のしかた

**ホーム画面への再追加は不要です。** アイコンから開けば、オンライン時に自動で最新版へ切り替わります（v1.8.0以降）。

更新が反映されないときは、次の順に試してください。

1. アプリ内の **設定 → 「🔄 最新版に更新する」** を押す（学習履歴は消えません）
2. Safari で `https://<ユーザー名>.github.io/toeic-vocab/?v=2` のように末尾に `?v=数字` を付けて開く（数字は毎回変える）
3. それでも直らない場合のみ、設定 → Safari → 詳細 → Webサイトデータ から該当サイトを削除
   （※ 学習履歴も消えるため、事前に設定画面からバックアップを取ること）

開発側では、ファイルを更新したら `sw.js` の `CACHE` のバージョンを必ず上げてください。

## 更新履歴

- **v3.8.0 (2026-07-29 14:20 JST)**
  - **シャドーイング機能を新設**（`shadowing.html` + `shadowing.json`）。モードメニューから開きます。
    - 再生ボタンで開始、速度 0.5〜1.3倍、英文/和訳の表示ON/OFF、読み上げに合わせた**語単位のカラオケ式ハイライト**。
    - 練習ステップ（聞く→重ねる→追う→再現）のワンタップ切替、文タップ再生、文ごとのくり返し、文間の無音、ループ、練習回数の記録。
    - 教材は初版5本（108〜180語 / 約43〜72秒）。すべて1文ずつ和訳・キーフレーズ・音のポイント付き。
  - 実装メモ：再生は「1文＝1発話」単位。文中の語位置は `onboundary` が来る環境ではイベント基準、来ない環境（iOS Safari）では経過時間から推定し、実測した読み上げ速度で毎回自動補正します。`cancel()` が中断発話の `onend` を発火させる仕様による「文の二重送り」は世代番号で防いでいます。
  - 別ページにした理由：`index.html` が既に大きく、音声再生の状態管理を単語学習と混ぜると副作用が出やすいため。
  - `sw.js` のキャッシュ名を `toeic-vocab-v3.8.0` に更新し、シャドーイングの2ファイルを事前キャッシュ対象に追加。
- **v3.7.0 (2026-07-29 10:00 JST)**
  - **単語Tips機能を新設。** カード裏と単語一覧の詳細に「🔍 使い分け」「🔗 意味のつながり」のボタンが出て、タップすると解説がポップアップします。
    - 追加の経緯: 「日本語では同じ訳になるが微妙にニュアンスが違う語」「同じ語幹なのに意味の繋がりに疑問が出る語」をその場で解消できるようにするため。
    - 初版は **89件** のTipsを収録し、既存語彙の **280語** に紐づけました（使い分け57件 / 意味のつながり32件）。
    - 従来の 💡 語源解説（`etymology`）は1語1解説なのに対し、Tipsは1件を複数語に紐づけられるため、比較系の解説を重複なく持てます。両者は併存します。
  - データ形式: `word-relations.json` にトップレベルキー `tips` を追加。`[{ id, kind:"nuance"|"origin", title, terms:[語...], body }]`。起動時に「語 → Tips」の逆引き索引を組み立てます。
  - Tipsのある語には単語一覧に `Tips` バッジを表示。
  - `sw.js` のキャッシュ名を `toeic-vocab-v3.7.0` に更新。
- **v3.6.0 (2026-07-24 15:00 JST)**
  - Part6にタイマーバーを追加（1問30秒・1文書4問で約2分の目安。経過しても自動遷移・採点はしない）。
  - 左上のモード切替を「循環」から「選択式メニュー」に変更。単語を一番上に、パートは数字順（単語 / Part5 / Part6）で並べ、タップで直接選べるようにした。
  - **Part6を本番仕様に変更**：従来は空所を1つずつ解いて即座に正誤が出たが、本番のPart6は4問すべてを正解が分からない状態で解く。そこで4空所すべてを選んでから「まとめて採点」する方式に変更。採点前は空所間を自由に行き来して選び直せる。
  - **Part6の採点結果画面**：上部に全空所の正解を埋め込んだ本文を固定表示。中央に空所ごとの解説カードを置き、左右スワイプ（またはドット/ボタン）で空所1〜4を切り替え。各カードはPart5と同じく正誤・正解・各選択肢の説明・目安スコアを表示。下部の頻出語と全文和訳はスワイプ不要の共通表示。
  - Part6文書を5→**75文書（300問）**に拡張。1セッション3文書なので全**25セッション**。
- **v3.5.0 (2026-07-24 13:30 JST)**
  - **TOEIC Part6 演習モードを新設（5文書＝20問で試作）。** ヘッダーのモード切替を「単語→Part5→Part6→単語」の3循環に拡張。Part6は1文書に空所4つで、文脈を踏まえて各空所を4択で解く。カテゴリは語彙・文法・接続語・文選択（Part6特有の一文選択問題）。本文を表示し空所をハイライト、各空所を解くごとに正解の理由・各選択肢の説明・目安スコアを表示。文書の最後の空所を解いたら頻出語と全文和訳を表示。
  - Part6の卒業・再出題は**文書単位**。4問すべて正解できた回を1カウントし、3回連続で全問正解したら文書ごと卒業。1問でも間違えたらカウントを0にリセットし同セッション内で再挑戦。全問正解1回→翌学習日、2回→3日後に再出題。
  - 1セッション＝**3文書（12問）**。卒業した文書を確認できる「卒業ボックス」も新設。問題データは `part6.json`。
- **v3.4.0 (2026-07-24 12:00 JST)**
  - Part5問題を30問→**330問**に大幅追加（新規300問）。文法・品詞・語彙をバランスよく（品詞112/文法108/語彙110）、600/730/860点レベルを混在。
  - **改善1**: 目安スコア(level)を問題画面に表示せず、解説画面にのみ表示。
  - **改善2**: 問題画面に20秒の目安タイマーバーを追加。20秒経過しても自動遷移・正誤確定はせず、あくまで目安。
  - **改善3**: 再出題ロジック（連続正解カウント方式）。3回連続正解で「卒業」。不正解で連続正解を0にリセットし同セッション内で再出題。正解時は1回→翌学習日、2回→3日後に再出題（朝6時基準）。進捗は端末に保存。
  - **改善4**: 1セッション=**10問**に分割（330問→33セッション）。セッション選択画面と進捗バーを追加。
  - 卒業した問題を確認できる「卒業ボックス」画面を新設。
- **v3.3.0 (2026-07-24 10:30 JST)**
  - **TOEIC Part5 演習モードを新設（30問で試作）。** ヘッダー左上のボタンで「単語」⇄「Part5」を切り替え。短文の空所補充4択問題で、文法・品詞・語彙をバランス良く、難易度は600/730/860点レベルを混在（各問に目安表示）。解答後に、正解の理由・各選択肢の説明（品詞・意味・なぜ不正解か）・問題文の頻出語・全文和訳を表示。全問レベル別/カテゴリ別に出題可能。問題データは `part5.json`。
- **v3.2.0 (2026-07-24 09:00 JST)**
  - 新規単語 第2弾（169語）を `toeic-new2.json` として追加。合計2860語。既存2691語すべてと重複なし、全語に和訳・例文・例文和訳つき、カタカナのみ訳なし、複数品詞は併記、を検証済み。インポート画面に「追加169問（第2弾）」ボタンを新設。既存語が広範なため、品質優先で169語に確定した。
  - 単語検索欄の日本語入力バグを修正。従来は入力のたびに画面全体を再描画していたため、日本語のIME変換中に入力欄が作り直され、二重入力・変換不可・削除不良が起きていた。入力中はリスト部分だけを更新する方式に変更。
  - 検索対象を拡張（英単語・意味・例文・例文和訳・読み方）。
  - 派生語データを新語込みで再生成（375→402語）。
- **v3.1.0 (2026-07-23 22:10 JST)**
  - カード裏の「同じ語根の仲間」に各語の日本語の意味を併記。派生語をその場でまとめて覚えられる。表示を縦積みリスト（単語／品詞／意味）に変更。
- **v3.0.0 (2026-07-23 21:55 JST)**
  - 「今日解いた語」の一覧でも、語をタップすると裏面（答え）の詳細を表示。詳細から「← 一覧に戻る」で一覧へ戻れる。
- **v2.9.0 (2026-07-23 21:40 JST)**
  - 語源解説を44語→69語に拡充。
  - 解説内で挙げる同語根の単語すべてに日本語の意味を併記。語根の共通性だけでなく各語の意味も同時に確認できる（例:「fer(運ぶ)の仲間: refer(参照する)、offer(提供する)、confer(協議する)」）。
- **v2.8.0 (2026-07-23 21:20 JST)**
  - 語源解説を10語→44語に拡充。接頭辞・語根から意味を読み解ける語を追加し、共通の語根を持つ語（mit=送る、pos=置く、tain=保つ、fer=運ぶ、ceed=行く 等）をまとめて覚えられるように相互言及した。
- **v2.7.0 (2026-07-23 21:00 JST)**
  - 単語一覧で語をタップすると、学習時の裏面（答え）と同じ詳細をポップアップ表示。品詞・意味・例文・和訳・発音・派生語・語源解説をまとめて確認できる。編集ボタンも配置。
- **v2.6.0 (2026-07-23 20:40 JST)**
  - **派生語をまとめて学習する機能。** カード裏に「同じ語根の仲間」を品詞つきで表示（例: `exclusive` の裏に `exclusively`）。181グループ375語を収録。
  - **語源解説（代表10語で試行）。** 意味が派生・変化した語に、語幹のイメージからの解説を追加。💡 をタップでポップアップ表示。例: `appreciate`（価値を認める→感謝する/理解する/値上がりする）、`comprehend`（つかむ→理解する）。
  - **「今日解いた語」機能。** 学習した日と正誤を記録し、ホームから当日分の一覧（正誤つき）を見返せる。
- **v2.5.0 (2026-07-23 20:05 JST)**
  - 複数品詞を持つ語の品詞・意味を補完（108語）。例: `account`「名詞」→「名詞/動詞」、`present`→「名詞/形容詞/動詞」。英語として複数品詞を持ち、TOEICでその用法が問われる語のみを対象とし、意味も品詞に対応させて併記。
  - 取り込み済みカードには、設定 →「既存の語の和訳・意味を最新に更新」で反映できる。
- **v2.4.0 (2026-07-23 19:40 JST)**
  - **例文と和訳が食い違う不具合を修正。** 初期サンプル10語に和訳がなかったため、和訳マージ時に同じ見出し語の別例文の訳が誤って付いていた（例: `on behalf of`）。サンプルに正しい和訳を付与し、マージの照合条件に例文の一致を追加。既に誤訳が付いたカードを修復する処理も追加した。
- **v2.3.0 (2026-07-23 19:25 JST)**
  - カタカナだけの訳を日本語の説明入りに修正（全28語）。読み方を示すだけで意味の説明になっていなかったため。例: `presentation`「プレゼンテーション」→「発表/提示」。カタカナ語として定着しているものは補足として残した。
  - 和訳マージ機能が意味(meaning)の更新にも対応。取り込み済みのカードにも修正を反映できる（設定 →「既存の語の和訳・意味を最新に更新」）。
- **v2.2.0 (2026-07-23 19:10 JST)**
  - **出題の仕組みを「学習日」単位に変更。** 1日の境目を朝6時とし、同じ日なら何時に開いても同じ顔ぶれが出るように。
  - 出題順を「復習分 → 再挑戦分 → 新規」に変更。翌日は前日の正解分の復習から始まり、前日の不正解分、その後に続きの新規語へ進む。
  - 不正解の語を当日繰り返す挙動を廃止し、翌日回しに変更（復習間隔を確保するため）。
  - 1日の新規語数の上限を実際に機能させた（復習・再挑戦は対象外）。
  - 和訳マージの対象に全データファイルを追加。取り込み済みで和訳のないカードを後から補えるように。
- **v2.1.0 (2026-07-23 18:55 JST)**
  - 新規1000語を `toeic-new1000.json` として追加（全て和訳付き、既存1691語と重複ゼロ）。同梱データは合計2691問に。
  - 内訳: 熟語450 / 動詞229 / 名詞145 / 形容詞144 / 副詞32。人事・財務・IT・マーケティング・物流・オフィス業務・会議・出張などTOEIC頻出の実務場面を網羅。
- **v2.0.0 (2026-07-23 18:40 JST)**
  - 法務・契約・製造・品質管理テーマの137問を `toeic-extra2.json` として追加（和訳付き）。同梱データは合計1691問に。
  - 点検結果: 提供232件中、62件が既存と重複、33件を品質基準で除外（実在しない語・TOEIC範囲外の専門語・文形式のもの）。
- **v1.9.0 (2026-07-23 18:15 JST)**
  - 外部AI生成データ287件を点検し、既存と重複しない54件を `toeic-extra.json` として追加（和訳付き）。同梱データは合計1554問に。
  - 点検結果: 287件中229件が既存1500問と重複、4件が表記ゆれによる実質重複のため除外。品詞・例文の誤りはなし。
- **v1.8.0 (2026-07-23 10:10 JST)**
  - **アプリが更新されない問題を修正。** Service Worker を全面改訂し、HTML/JSONは常にネットワーク優先で取得するように変更。起動・復帰のたびに新版を確認し、自動で切り替わる。
  - 設定画面に「🔄 最新版に更新する」ボタンを追加（学習履歴は保持）。
- **v1.7.0 (2026-07-23 09:50 JST)**
  - 例文の和訳(exampleJa)に対応。同梱の500問・1000問すべてに日本語訳を付与。
  - 学習履歴を保ったまま既存カードに和訳だけを追加できる機能（設定→「既存の語に例文の和訳を追加」）。
  - バックアップ/復元機能を追加。学習履歴ごとJSONで保存・読み戻しが可能に。
- **v1.6.0 (2026-07-23 09:35 JST)**
  - 発音機能を追加。裏面のスピーカーボタンで単語・例文を読み上げ（Web Speech API、オフライン可）。
  - 音声は端末内蔵のものを使用。英語音声(en-US)を優先し、無ければ既定音声を使用。
- **v1.5.0 (2026-07-23 09:20 JST)**
  - 追加データ `toeic-1000.json`（既存500と重複しない1000問）を追加。合計1500問に。
  - インポート画面に「追加1000問を追加」ボタンを新設。`sw.js` のCACHEを更新。
- **v1.4.0 (2026-07-23 02:10 JST)**
  - 出題順をボックスの大きい順（Box7→Box1）に変更。同一ボックスは期限が古い順。
  - JSONを貼り付けて取り込む導線を追加（AI出力の ``` 囲みは自動除去）。AI用プロンプトを本READMEに追記。
- **v1.3.0 (2026-07-23 01:52 JST)**
  - JSON一括取り込みに対応。「TOEIC頻出500問を追加」ボタンと自作JSON読み込みを追加。重複は自動スキップ。
  - 同梱データ `toeic-500.json`（500語/熟語）を追加。オフラインキャッシュ対象にも追加し、`sw.js` のCACHEを更新。
- **v1.2.0 (2026-07-23 01:21 JST)**
  - PWA化（ホーム画面インストール・オフライン対応）。manifest.json / sw.js / アイコンを追加。
  - 保存先を `window.storage` から `localStorage` へ変更し、端末内に永続保存。
- **v1.1.0 (2026-07-23 01:11 JST)**
  - 単語カードに品詞を追加。めくった裏面に品詞を表示。
  - 追加/編集・一括インポート・エクスポートを品詞対応に。
  - ストレージ層を耐障害化。
- **v1.0.0 (2026-07-23)**
  - 初版。Leitner間隔反復・単語管理・出題周期の設定。
